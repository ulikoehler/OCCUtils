#pragma once
#include <gp_Pnt.hxx>

namespace OCCUtils {
    namespace Point {
        /**
         * Return the global coordinate system's origin:
         * (0,0,0)
         */
        gp_Pnt Origin(); 
    }
}